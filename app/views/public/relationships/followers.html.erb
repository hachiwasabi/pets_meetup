<div class="container my-5">
  <h1 class="mb-4 custom-orange text-center">🐡 フォロワー一覧</h1>

  <% if @users.exists? %>
    <div class="row g-4">
      <% @users.each do |user| %>
        <div class="col-sm-6 col-md-4 col-lg-3">
          <div class="card shadow-sm h-100 custom-card text-center p-3">
            <%= link_to user_path(user) do %>
              <%= image_tag user.profile_image.variant(resize_to_limit: [150, 150]).processed,
                            class: "img-fluid rounded-circle mb-2 profile-image" %>
            <% end %>
            
            <h5 class="fw-bold mb-1 custom-orange">
              <%= link_to user.name, user_path(user), class: "group-link" %>さん
            </h5>
            <p class="text-muted small mb-0"><%= user.pet_type %>：<%= user.pet_name %>ちゃん</p>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <h4 class="text-center text-muted">ユーザーはいません。</h4>
  <% end %>
</div>

<div class="mt-3">
  <%= paginate @users %>
</div>