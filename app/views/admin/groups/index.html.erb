<div class="container my-4">
  <h2 class="mb-3">グループ一覧（管理者）</h2>

  <table class="table table-bordered">
    <thead class="table-light">
      <tr>
        <th>ID</th>
        <th>グループ名</th>
        <th>作成者</th>
        <th>メンバー数</th>
        <th>作成日</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <td><%= group.id %></td>
          <td><%= group.name %></td>
          <td><%= group.owner.name %></td>
          <td><%= group.group_members.approved.count %>人</td>
          <td><%= group.created_at.strftime("%Y-%m-%d") %></td>
          <td>
            <%= button_to "削除", admin_group_path(group), method: :delete, 
                data: { confirm: "本当に削除しますか？ グループと関連するデータがすべて削除されます。" }, 
                class: "btn btn-danger btn-sm" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to "管理トップへ戻る", admin_users_path || admin_users_path, class: "btn btn-secondary mt-3" %>
</div>
